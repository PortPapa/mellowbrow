Warm white container with soft elevation — wraps services, reviews, info blocks.

```jsx
<Card elevation="sm" interactive>
  <h3>자연눈썹</h3>
  <p>결을 살린 자연스러운 눈썹</p>
</Card>
```

Props: `elevation` (none/xs/sm/md/lg), `padded` (default true), `interactive` (hover lift).
