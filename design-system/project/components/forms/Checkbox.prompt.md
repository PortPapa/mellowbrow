Soft square checkbox — consent, options, agreement.

```jsx
<Checkbox checked={ok} onChange={setOk} label="개인정보 수집·이용에 동의합니다" />
```

Controlled via `checked` + `onChange(next)`. Optional `label`, `disabled`.
